{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "cs": "changeset",
    "release": "changeset publish",
    "version": "changeset version",
    "verify": "turbo run test"
  },
  "devDependencies": {
    "@changesets/cli": "^2.25.2",
    "turbo": "^1.6.3"
  }
}
