{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "cs": "changeset",
    "require": "pnpm --filter uni-require",
    "release": "changeset publish",
    "version": "changeset version",
    "verify": "turbo run test"
  },
  "devDependencies": {
    "@changesets/cli": "^2.25.2",
    "turbo": "^1.6.3"
  },
  "packageManager": "pnpm@7.27.1"
}
